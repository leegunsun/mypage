import styled from "styled-components";

export const footerContainer = styled.img`
  transform: scaleY(-1);
  width: 100%;
  height: 200px;
  object-fit: cover;
  position: relative;
  bottom: 0;
  left: 0;
`;

export const testfooter = styled.footer``;
